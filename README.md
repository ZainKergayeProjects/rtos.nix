# rtos nix flake

This flake is here to aid development of embedded RTOS applications. To use in Nix, simply start with an already created git repo and use `nix flake init -t github:ZainKergayeProjects/rtos.nix`
